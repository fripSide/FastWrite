修改写作：

右边界面，最上面是修改内容选择，可以有：section级别、段级别、句子级别。根据这三个级别把文本拆成一个个item。

选择一个item之后，可进行精细修改：

三种模式，dignose，refine，quick fix。可以都用同一个llm调用函数。只是prompt不同，然后调用的模型也可能不同。可以设置为选择。


写作之后，需要展示具体每个词级别的diff，反应出具体修改。例如：左边是diff，右边是新版本编辑框，这时用户可以选择继续修改，还是apply。



latex符号处理比较复杂，应该原原本本显示在html界面中。
编辑的时候应该始终保留这些符号。